<div class="container-fluid">
    <header class="header">

        <h1>oh! Last Night</h1>
        <span class=" cloud glyphicon glyphicon-cloud"></span>
        <br>

        <button class="btn btn-primary btn-lg" type="button" data-toggle="collapse" data-target="#newPost" aria-expanded="false" aria-controls="collapseExample">
            <span class="glyphicon glyphicon-plus"></span>
            Create a post
        </button>
        <div class="collapse" id="newPost">
            <div class="create_new">
                <%= form_for @post do |f| %>
                <%= f.text_field :title, :maxlength => 40, :required => true, placeholder: "Clever title goes here" %>
                <br>
                <%= f.text_area :body, :maxlength => 1500, :required => true, placeholder: "Alright, let's hear it:" %>
                <br>
                <button class="btn btn-success" type="button"><%= f.submit "Post" %></button>
                <% end %>
            </div>
        </div>

        <button class="btn btn-danger btn-lg" type="button" data-toggle="collapse" data-target="#about" aria-expanded="false" aria-controls="collapseExample">
            <span class="glyphicon glyphicon-question-sign"></span>
            About
        </button>
        <div class="collapse" id="about">
            <div class="well">
                <p>Simple.</p>
                <p>You have up to 1500 characters to tell a story.</p>
                <p>Walk of shame, monumental achievement, falling down a flight of stairs, whatever ya got.</p>
                <p>20 posts are shown at a time then they're deleted.</p>
                <p>There's no usernames or anything like that, dont take this serious.</p>
                <p>Just share, make fun of, relate and/or enjoy.</p>
            </div>
        </div>
    </header>

    <section class="container">
        <% @posts.each do |post| %>
        <div class="post_stuffins">
            <h2>
                -
                <%= post.title %></h2>
            <p><%= post.body %></p>
            <br>
            <div class=time><%= post.created_at.strftime("Created on a %A") %></div>
        </div>
        <% end %>
    </section>
</div>

<footer class="footer">
    <h3>O!LN
        <span>&copy 2016</span>
    </h3>
</footer>